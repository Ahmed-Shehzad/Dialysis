namespace Dialysis.DomainDrivenDesign.DomainServices;

/// <summary>
/// Marker for domain-layer services: stateless (or identity-scoped) operations that coordinate
/// aggregates, value objects, or repositories when the behavior does not belong on a single type.
/// </summary>
public interface IDomainService
{
}
