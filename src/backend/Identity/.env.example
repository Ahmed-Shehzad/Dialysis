# Copy to .env and adjust. Do not commit real secrets.
POSTGRES_PASSWORD=keycloak
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin
KEYCLOAK_HTTP_PORT=8080

# Dialysis.Identity.Bff (user-secrets or environment)
# Identity__Keycloak__ClientSecret must match Keycloak client "dialysis-bff" secret (see RUNBOOK.md after first import).
