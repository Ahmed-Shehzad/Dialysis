# Example ConfigMap – replace values for your environment
apiVersion: v1
kind: ConfigMap
metadata:
  name: dialysis-config
  namespace: dialysis-pdms
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  ASPNETCORE_URLS: "http://+:8080"
  # Override with actual values; secrets should come from Secret resources
  Auth__Authority: "https://your-idp.example.com/realms/dialysis"
  Auth__Audience: "dialysis-api"
