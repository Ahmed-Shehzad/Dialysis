---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "patient-portal-web-config"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "patient-portal-web"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
data:
  NODE_ENV: "{{ .Values.config.patient_portal_web.NODE_ENV }}"
  GATEWAY_HTTP: "{{ .Values.config.patient_portal_web.GATEWAY_HTTP }}"
  services__gateway__http__0: "{{ .Values.config.patient_portal_web.services__gateway__http__0 }}"
  GATEWAY_HTTPS: "{{ .Values.config.patient_portal_web.GATEWAY_HTTPS }}"
  BROWSER: "{{ .Values.config.patient_portal_web.BROWSER }}"
  VITE_GATEWAY_URL: "{{ .Values.config.patient_portal_web.VITE_GATEWAY_URL }}"
  VITE_API_BASE_URL: "{{ .Values.config.patient_portal_web.VITE_API_BASE_URL }}"
  PORT: "{{ .Values.config.patient_portal_web.PORT }}"
