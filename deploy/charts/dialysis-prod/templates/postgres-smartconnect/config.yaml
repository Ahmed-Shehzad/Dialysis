---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "postgres-smartconnect-config"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "postgres-smartconnect"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
data:
  POSTGRES_HOST_AUTH_METHOD: "{{ .Values.config.postgres_smartconnect.POSTGRES_HOST_AUTH_METHOD }}"
  POSTGRES_INITDB_ARGS: "{{ .Values.config.postgres_smartconnect.POSTGRES_INITDB_ARGS }}"
  POSTGRES_USER: "{{ .Values.config.postgres_smartconnect.POSTGRES_USER }}"
