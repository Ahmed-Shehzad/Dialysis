---
apiVersion: "apps/v1"
kind: "StatefulSet"
metadata:
  name: "postgres-pdms-statefulset"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "postgres-pdms"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "{{ .Chart.Name }}"
        app.kubernetes.io/component: "postgres-pdms"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      containers:
        - image: "docker.io/timescale/timescaledb:latest-pg17"
          name: "postgres-pdms"
          envFrom:
            - configMapRef:
                name: "postgres-pdms-config"
            - secretRef:
                name: "postgres-pdms-secrets"
          ports:
            - name: "tcp"
              protocol: "TCP"
              containerPort: 5432
          resources:
            limits:
              cpu: "1000m"
              memory: "2Gi"
            requests:
              cpu: "250m"
              memory: "512Mi"
          volumeMounts:
            - name: "dialysis-postgres-pdms-data"
              mountPath: "/var/lib/postgresql/data"
          imagePullPolicy: "IfNotPresent"
      volumes:
        - name: "dialysis-postgres-pdms-data"
          emptyDir: {}
  selector:
    matchLabels:
      app.kubernetes.io/name: "{{ .Chart.Name }}"
      app.kubernetes.io/component: "postgres-pdms"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  replicas: 1
  persistentVolumeClaimRetentionPolicy: {}
  updateStrategy:
    rollingUpdate: {}
    type: "RollingUpdate"
