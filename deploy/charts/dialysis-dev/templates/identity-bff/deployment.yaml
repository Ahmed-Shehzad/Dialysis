---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "identity-bff-deployment"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "identity-bff"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "{{ .Chart.Name }}"
        app.kubernetes.io/component: "identity-bff"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      containers:
        - image: "{{ .Values.parameters.identity_bff.identity_bff_image }}"
          name: "identity-bff"
          envFrom:
            - configMapRef:
                name: "identity-bff-config"
            - secretRef:
                name: "identity-bff-secrets"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort: 5275
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "{{ .Chart.Name }}"
      app.kubernetes.io/component: "identity-bff"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
