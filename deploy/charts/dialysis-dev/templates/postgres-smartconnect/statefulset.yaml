---
apiVersion: "apps/v1"
kind: "StatefulSet"
metadata:
  name: "postgres-smartconnect-statefulset"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "postgres-smartconnect"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "{{ .Chart.Name }}"
        app.kubernetes.io/component: "postgres-smartconnect"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      containers:
        - image: "docker.io/postgres:17-alpine"
          name: "postgres-smartconnect"
          envFrom:
            - configMapRef:
                name: "postgres-smartconnect-config"
            - secretRef:
                name: "postgres-smartconnect-secrets"
          ports:
            - name: "tcp"
              protocol: "TCP"
              containerPort: 5432
          resources:
            limits:
              cpu: "500m"
              memory: "1Gi"
            requests:
              cpu: "100m"
              memory: "256Mi"
          volumeMounts:
            - name: "dialysis-postgres-smartconnect-data"
              mountPath: "/var/lib/postgresql/data"
          imagePullPolicy: "IfNotPresent"
      volumes:
        - name: "dialysis-postgres-smartconnect-data"
          emptyDir: {}
  selector:
    matchLabels:
      app.kubernetes.io/name: "{{ .Chart.Name }}"
      app.kubernetes.io/component: "postgres-smartconnect"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  replicas: 1
  persistentVolumeClaimRetentionPolicy: {}
  updateStrategy:
    rollingUpdate: {}
    type: "RollingUpdate"
