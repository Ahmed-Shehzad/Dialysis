---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "hie-web-config"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "hie-web"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
data:
  NODE_ENV: "{{ .Values.config.hie_web.NODE_ENV }}"
  BROWSER: "{{ .Values.config.hie_web.BROWSER }}"
  VITE_GATEWAY_URL: "{{ .Values.config.hie_web.VITE_GATEWAY_URL }}"
  VITE_API_BASE_URL: "{{ .Values.config.hie_web.VITE_API_BASE_URL }}"
  PORT: "{{ .Values.config.hie_web.PORT }}"
