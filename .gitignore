## Build outputs
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/
artifacts/

## NUKE build (runtime scratch; parameters.json + build.schema.json stay tracked)
.nuke/temp/

## .NET / MSBuild
*.user
*.userosscache
*.suo
*.sln.docstates
*.userprefs
project.lock.json
project.fragment.lock.json
*.nuget.props
*.nuget.targets
*.csproj.user
*.vbproj.user
*.fsproj.user
*.dbmdl
*.jfm
*.pfx
*.publishsettings
orleans.codegen.cs

## NuGet
*.nupkg
*.snupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/

## Rider / JetBrains
.idea/
*.sln.iml
*.lscache

## Visual Studio
.vs/
*.rsuser

## VS Code (keep shared config if present)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

## Claude Code (local agent state)
.claude/

## SonarQube (local scanner state)
.sonarqube/

## Test & coverage
## Playwright e2e artifacts (videos, screenshots, traces, HTML reports) — one subtree per SPA under
## e2e-artifacts/<app>/. Regenerated (and overwritten) on every `npm run test:e2e`; never committed.
e2e-artifacts/
TestResults/
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml
coverage*.json
coverage*.xml
*.opencover.xml

## macOS
.DS_Store
.AppleDouble
.LSOverride

## Windows
Thumbs.db
ehthumbs.db
Desktop.ini

## Local / secrets (never commit)
.env
.env.*
!.env.example
*.local.json
appsettings.*.local.json
appsettings.Local.json
secrets.json

## NCrunch / dotCover
_NCrunch_*
.*crunch*.local.xml
nCrunchTemp_*
*.dotCover

## Azure Functions local
local.settings.json

## Misc editors
*.swp
*.swo
*~
*.bak

## Node (operator-shell TS build)
node_modules/
.cache/


# Added by ggshield
.cache_ggshield

## Aspire intermediate manifest emitted alongside the generated deploy artifacts. Nothing consumes
## it (Docker Compose reads docker-compose.yaml; Helm reads Chart.yaml + values.yaml + templates/),
## and the k8s one is non-deterministic until the Aspire duplicate-DeploymentTargetAnnotation bug is
## fixed — so it stays an untracked byproduct, excluded from the deploy-artifacts drift gate.
deploy/compose/*/aspire-manifest.json
deploy/charts/*/aspire-manifest.json
